# CARLA Python API — install from CARLA release package
# Download from: https://github.com/carla-simulator/carla/releases/tag/0.9.16
# Then: pip install <path>/carla-0.9.16-cp3x-cp3x-win_amd64.whl
# The 0.9.16 wheels are cp312 only, so the venv must be Python 3.12.
# carla==0.9.16

# Required by CARLA's own agents package, which pathfinder/sim/carla_backend.py
# imports for route planning (agents.navigation.global_route_planner). It ships
# with the CARLA release but is not declared by the wheel, so a fresh venv hits
# ModuleNotFoundError on the first routed episode.
networkx>=3.0

# Required by CARLA's own agents/navigation/basic_agent.py, which
# CarlaBehaviorAgentPolicy (pathfinder/policies.py) instantiates via
# BehaviorAgent. Same situation as networkx above: ships with the CARLA
# release, not declared by the wheel, ModuleNotFoundError on the first
# episode that runs the reference baseline (#16).
shapely>=2.0

# Deep learning
torch>=2.0.0
torchvision>=0.15.0
ultralytics>=8.4.0

# Numerical / vision
numpy>=1.24.0
opencv-python>=4.8.0
Pillow>=10.0.0

# Localization (EKF is implemented from scratch with numpy, no filterpy needed)

# GNSS coordinate conversion (implemented inline, no external dep)

# Data collection / training utilities
tqdm>=4.65.0

# Visualization / HUD
pygame>=2.5.0

# Configuration
PyYAML>=6.0

# gRPC (worker coordination: pathfinder/rpc/)
grpcio>=1.83.0
grpcio-tools>=1.83.0

# AWS SDK (pathfinder/cloud/: SQS, S3, Kinesis, SageMaker backends)
boto3>=1.43.0

# Parquet telemetry warehouse (pathfinder/cloud/warehouse.py)
pyarrow>=25.0.0
