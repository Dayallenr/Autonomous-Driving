# Dev/test-only dependencies — not needed to run the app, only to test and
# lint it. Installed on top of requirements.txt.
-r requirements.txt

# Test runner. CI invokes `pytest` directly, so it has to be declared here
# rather than assumed present in the developer's environment.
pytest>=8.0.0

# Linting
ruff>=0.6.0

# Mocked-AWS tests for pathfinder/cloud/ (SQS, S3, Kinesis) — lets CI exercise
# the boto3-backed code paths without real credentials or LocalStack.
moto[sqs,s3,kinesis]>=5.0.0

# Terraform static analysis (Phase 7) is checkov, but it is deliberately NOT
# listed here: checkov pins boto3==1.35.49, which cannot resolve alongside
# this project's boto3>=1.43.0 and broke `pip install -r requirements-dev.txt`
# outright. CI runs checkov via bridgecrewio/checkov-action in its own
# environment (see .github/workflows/ci.yml); to run it locally, keep it
# isolated the same way: `pipx install checkov` (or its own venv).
